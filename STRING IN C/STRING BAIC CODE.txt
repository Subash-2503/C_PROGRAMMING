#include <stdio.h>

int main()
{
  //char str[10];
 // scanf("%s",str);
 // scanf("[^\n",str);
 // gets(str);
  //fgets(str,10,str);
  for(int i=0;i<10;i++)
  {
      scanf("%c",&str[i]);
  }
  for(int i=0;i<10;i++)
  {
     strlength=str[i]
      
  }
      printf("%s",str);
}